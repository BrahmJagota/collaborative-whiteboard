interface IPrevCords {
    x: number,
    y: number
}

interface IShape {
    x: number,
    y: number,
    width: number,
    height: number,
    color: string
}
interface IUserProps {
    userId: string,
    email: string
}